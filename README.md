# Coup RL Bot

The public browser demo is the **Gen5 1v1** bot and is staging-only until the Render
service passes the two-game isolation smoke test. The deployment manifest lives in the
`austxu.dev` portfolio repository; it configures the Render Free service, exact origin
allowlist, health endpoint, and generated secret.
